import logging


logger = logging.getLogger(__name__)


def forced_extraction(sources):
    """
    check and extract expected measurements, and associated them with the
    related source(s)
    """
    pass
